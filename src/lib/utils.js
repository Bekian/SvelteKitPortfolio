/**
 * Toggles the dropdown visibility.
 * Just inverts the boolean value
 *
 * @param {boolean} dropdownBool - The current state of the dropdown.
 * @returns {boolean} - The updated state of the dropdown after toggling.
 */
export function toggleDropdown(dropdownBool) {
    return !dropdownBool
}